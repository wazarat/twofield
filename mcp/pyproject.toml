[project]
name = "twofield-mcp"
version = "0.1.0"
description = "Twofield MCP server"
requires-python = ">=3.11"
dependencies = [
  "fastmcp>=3.4.6,<4",
  "httpx>=0.28,<1",
]

[tool.uv]
package = false

